#include "string.h"
#include <stdlib.h>
/*
char* pad(char* s, int d) {
	if (s == NULL) return NULL;
	int len = 0 char* ret_str = NULL;
	for (len; s[len] != '\0'; ++len) { }
	if (len == d) {
		char* copy = (char*)malloc((len+1) * sizeof(char));
		for (int i = 0; i < len; ++i) copy[i] = s[i];
		copy[i] = '\0';
		return copy;
	}
	else {
		ret_str = (char*)malloc();
	}
}	
*/

